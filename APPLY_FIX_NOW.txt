HARDWARE MIGRATION FIX - READY TO APPLY

All fixes committed. Your configuration now addresses:
✅ D-Bus mount ordering (prevents service cascade failures)
✅ Network interface updated to enp2s0
✅ Intel GPU packages removed (server has no iGPU)
✅ Intel ISH modules blacklisted
✅ Kernel params cleaned up for server hardware

QUICK APPLICATION STEPS:

1. VERIFY NETWORK INTERFACE FIRST (CRITICAL!)
   ```bash
   ip link show
   ```

   Expected: enp2s0

   If different, edit BEFORE rebuilding:
   - /mnt/nixos/etc/nixos/modules/machines/nixos/emily/configuration.nix line 40
   - /mnt/nixos/etc/nixos/modules/machines/nixos/_common/default.nix line 115

2. PULL LATEST CONFIG
   ```bash
   cd /mnt/nixos/etc/nixos
   git pull origin claude/debug-auth-hardware-issue-01WL6CBJQYKiXLbWwpyne8UU
   ```

3. TEST & APPLY
   ```bash
   nixos-rebuild dry-build
   # If no errors:
   nixos-rebuild boot
   ```

4. REBOOT
   ```bash
   exit
   reboot
   ```

WHAT THIS FIXES:
- D-Bus will start AFTER /var/lib is mounted
- Network will use correct interface (or fall back to DHCP)
- No GPU driver conflicts
- No Intel ISH delays
- System should boot to working login screen

See HARDWARE_MIGRATION_FIX.md for full technical details.
